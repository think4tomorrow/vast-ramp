{
    "name": "vastlink-wallet-server",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "node src/server.js",
        "dev": "nodemon src/server.js",
        "test": "cross-env NODE_ENV=test jest"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.5.0",
        "express": "^4.18.2",
        "helmet": "^7.0.0",
        "mongoose": "^7.0.0",
        "morgan": "^1.10.0"
    },
    "devDependencies": {
        "jest": "^29.5.0",
        "nodemon": "^3.0.0",
        "supertest": "^6.3.0",
        "cross-env": "^7.0.3"
    },
    "jest": {
        "testEnvironment": "node",
        "testMatch": ["**/tests/**/*.test.js"],
        "setupFiles": ["dotenv/config"]
    }
}
